<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <!-- Add Bootstrap CSS link -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    header {
      background-color: transparent; /* Removed background color */
      color: #fff;
      padding: 40px 0; /* Adjusted padding */
      text-align: center;
    }

    .container {
      background-color: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 400px;
      margin-top: 20px;
    }

    h1 {
      font-size: 28px;
      margin-bottom: 20px;
    }

    .role {
      color: #f06292;
    }

    p {
      font-size: 16px;
    }


    /* Add custom styles */
    .delete-button {
      background-color: #f06292;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
    }

    .delete-button:hover {
      background-color: #ec407a;
    }

    .btn {
      margin-top: 10px;
      color: #fff;
      background-color: #007bff;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      cursor: pointer;
    }

    .btn:hover {
      background-color: #0056b3;
    }

    .welcome-message {
      font-size: 48px;
      color: #F06292;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
    }
  </style>
</head>
<body>
  <%- include('navbar.ejs') %>
  <header>
    <h1 class="welcome-message">Welcome, <%= username %>!</h1>
  </header>
  
  
  
  
  <div class="container">
    <% if (role === 'admin') { %>
      <h1 class="role">Only admin can see this</h1>
    <% } %>
    <p>This is your profile page.</p>

    <form action="/user/delete-profile" method="post" onsubmit="return confirmDelete()">
      <button type="submit" class="delete-button">Delete Profile</button>
    </form>

    <script>
      function confirmDelete() {
        return confirm("Are you sure you want to delete your profile?");
      }
    </script>

    <!-- Button to navigate to the wishlist page -->
    <a href="/user/wishlist" class="btn">Go to Wishlist</a>

    <!-- Button to navigate to the watchlist page -->
    <a href="/user/watchlist" class="btn">Go to Watchlist</a>
    <a href="/user/eventlist" class="btn">Go to Eventlist</a>

  </div>

  <!-- Add Bootstrap JS scripts -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
